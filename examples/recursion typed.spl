
var p = []; // Type [Bool]

f(a,b,c) :: a Bool [Bool] -> a{ // type: A.a: a Bool [Bool] -> a
	Int x = 0;
	Bool y = False;
	if(b){
		x = f(10,False,[]);//Hier weet je het type van f nog niet goed genoeg!
//		print(x);
//		y = f(10,False,p);//Dit zou fout moeten gaan!
		y = f(False,False,[]); //Dit gaat wel goed als de types gedefinieerd zijn
		return a;
	} else {
		return a;
	}
}

main () :: -> Void {
    return;
}
